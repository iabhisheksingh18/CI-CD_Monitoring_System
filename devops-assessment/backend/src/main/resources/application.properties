spring.application.name=monitoring
server.port=8080

# MongoDB Configuration
spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/cicd_monitoring}

# JWT Configuration
jwt.secret=${JWT_SECRET:change-this-development-secret-before-production}
jwt.expiration=86400000

# AI Service Configuration
ai.service.url=http://fastapi-ai:8000/analyze-log

github.token=${GITHUB_TOKEN:}
